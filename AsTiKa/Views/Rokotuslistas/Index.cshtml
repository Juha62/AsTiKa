@model IEnumerable<AsTiKa.Models.Rokotuslista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rokotteen_nimi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tehosterokotuksen_antopäivä)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mahdolliset_sivuoireet_rokotuksista)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rokotteen_nimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tehosterokotuksen_antopäivä)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mahdolliset_sivuoireet_rokotuksista)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Anto_päivämäärä }) |
            @Html.ActionLink("Details", "Details", new { id=item.Anto_päivämäärä }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Anto_päivämäärä })
        </td>
    </tr>
}

</table>
