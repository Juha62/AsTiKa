@model IEnumerable<AsTiKa.Models.Vuokrasopimus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Huoneiston_numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sopijapuolen_tiedot)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Huoneiston_numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sopijapuolen_tiedot)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Vuokramäärä }) |
            @Html.ActionLink("Details", "Details", new { id=item.Vuokramäärä }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Vuokramäärä })
        </td>
    </tr>
}

</table>
